package server.commands;

import server.Server;

public class ShoutHandler implements CommandHandler {
    @Override
    public void execute(Server server, Server.PlayerHandler playerHandler) {
        //String message = playerHandler.getMessage();
        //String messageToSend = message.substring(6);
        //server.broadcast(playerHandler.getName(), messageToSend.toUpperCase());
    }
}
